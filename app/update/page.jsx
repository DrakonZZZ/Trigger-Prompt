import Update from './Update';

export const metadata = {
  title: 'Edit & Update',
  description: 'Edit your prompts.triggers here',
};

const page = () => {
  return <Update />;
};
export default page;
