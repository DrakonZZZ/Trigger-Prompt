import Create from './Create';

export const metadata = {
  title: 'Create A Trigger',
  description: 'Create your prompt or trigger here',
};

const page = () => {
  return <Create />;
};

export default page;
